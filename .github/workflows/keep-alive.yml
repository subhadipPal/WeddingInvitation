name: Keep Neon DB Alive

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app to keep DB active
        run: |
          curl -sf "${{ secrets.APP_URL }}/api/guests/ping" || true
